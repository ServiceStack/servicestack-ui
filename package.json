{
  "name": "@servicestack/ui",
  "version": "0.0.4",
  "description": "TypeScript Definitions for ServiceStack built-in UI's",
  "homepage": "https://github.com/ServiceStack/servicestack-ui",
  "types": "index.d.ts",
  "scripts": {
    "typecheck": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postversion": "git push && git push --tags",
    "typedoc:build": "typedoc --theme gitlab-wiki",
    "docs:clean": "sed -i '/^export declare var /d;/^export.*import(/d' ./*.d.ts",
    "docs:build": "api-extractor run --local --verbose && api-documenter markdown -i temp -o docs && node -e \"require('child_process').execSync(('cp '+path.join('config','custom','*')+' docs').replace(/^cp/,path.sep!='/'?'copy':'cp'))\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ServiceStack/servicestack-ui.git"
  },
  "keywords": [
    "servicestack",
    "ui",
    "locode",
    "api-explorer",
    "admin-ui"
  ],
  "bugs": {
    "url": "https://github.com/ServiceStack/Issues"
  },
  "author": "ServiceStack, Inc. (https://servicestack.net)",
  "license": "BSD-3-Clause",
  "devDependencies": {
    "typedoc": "^0.22.13",
    "typedoc-gitlab-wiki-theme": "^1.0.0",
    "typedoc-plugin-markdown": "^3.11.14"
  }
}
